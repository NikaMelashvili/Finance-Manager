import { FinanceDataResponse } from './finance-data-response';

describe('FinanceDataResponse', () => {
  it('should create an instance', () => {
    expect(new FinanceDataResponse()).toBeTruthy();
  });
});
